apiVersion: v1
kind: Service
metadata:
  name: python-app-service
  namespace: python-app
spec:
  selector:
    app.kubernetes.io/name: python-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
